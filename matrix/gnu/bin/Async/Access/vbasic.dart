

class Vbasic {
   late ArgumentError newline = ArgumentError("${'\n'}");
   VarArgs(xml, version) {
     //  implement VarArgs
     throw UnimplementedError("encoding=""UTF8""?>\n<m>9</m>\n`}''");
   }
}